# site (not yet implemented)

Will hold the Next.js marketing one-pager: headline, a `defineJob()` snippet,
a short "why" section, a deploy button, and a link to the repo. No pricing,
no multi-page structure. See [`../SPEC.md`](../SPEC.md).

Deliberately built last, after the scheduler and dashboard are stable.
